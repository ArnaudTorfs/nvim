local whichkey = require "which-key"
local M = {}

function M.setup()
    -- local mainKeymaps = {
    --     ["a"] = { "<Plug>Sneak_S", "Sneak Backward" },
    -- }
    --
    -- whichkey.register(mainKeymaps, {
    --     mode = { "n", "v" },
    --     prefix = nil,
    --     buffer = nil,
    --     silent = true,
    --     noremap = true,
    --     nowait = false,
    -- })
end

return M
